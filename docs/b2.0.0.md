<br/>

>[!WARNING|style: flat|label:  简要说明 ]
>
>- <span style='color:red'>将`IConfiguration`对象转换一个具体对象 [ 以面向对象的方式 - 使用配置 ]</span>
>
>  [<span style='color:#008B00'>[👓 NuGet - Microsoft.Extensions.Configuration.Binder ]</span>](https://www.nuget.org/packages/Microsoft.Extensions.Configuration.Binder/ ':target=_blank') 
>
>  [`namespace：Microsoft.Extensions.Configuration`]
>
>
>```csharp
># [ 叶节：原子配置项 ]
>public static class ConfigurationBinder
>{
>
>      public static T GetValue<T>(this IConfiguration configuration, string key, T defaultValue);
>          => (T)this.GetValue(configuration, typeof(T), key, defaultValue)
>
>      public static object GetValue(this IConfiguration configuration, Type type, string key, object defaultValue)
>      {
>
>            IConfigurationSection section = configuration.GetSection(key);
>            string value = section.Value;
>            if (value != null)
>            {
>
>                 /// <summary>
>                 /// 字符串转换目标类型 (type)
>                 ///   - 目标类型 object : 返回原始值 value
>                 ///   - 目标类型 byte[] : Convert.FromBase64String(value)
>                 ///   - 目标类型 Nullable<T> : 当原始值 value 非空则根据 T.TypeConverter 转换, 否则返回 Null
>                 ///
>                 ///   - [默认] 根据目标类型 TypeConverter 进行转换
>                 ///     TypeConverter converter = TypeDescriptor.GetConverter(type);
>                 ///     if (converter.CanConvertFrom(typeof(string)))
>                 ///     {
>                 ///
>                 ///     }
>                 ///
>                 /// </summary>
>                 return ConvertValue(type, value, section.Path);
>           }
>           return defaultValue;
>
>      }
>
>}
>
>
>```
>
><br/>

```csharp
var source = new Dictionary<string, string>
{
	 ["A:B:1"] = string.Empty,
	 ["A:B:2"] = "1",
	 ["A:B:3"] = "True"
};
IConfigurationRoot root = new ConfigurationBuilder()
							 .AddInMemoryCollection(source)
							 .Build();

// string.Empty
Console.WriteLine(root.GetValue<object>("A:B:1"));

// 1
Console.WriteLine(root.GetValue<int>("A:B:2"));
Console.WriteLine(root.GetValue<Nullable<int>>("A:B:2").Value);

// True
Console.WriteLine(root.GetValue<bool>("A:B:3"));


```

