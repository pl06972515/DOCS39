>[!WARNING|style: flat|label: 简要说明 ]
>
>- (`Jenkins`) 是一个开源的自动化服务器，主要用于持续集成(`CI`）和持续交付/部署(`CD`）
>
>  <span style='color:red'>[ 它可以帮助开发者自动化构建、测试和部署软件项目，从而提高开发效率和软件质量 ]</span>
>
>  [<span style='color:#008B00'>[👓 GitHub ]</span>](https://github.com/jenkinsci/jenkins ':target=_blank') [<span style='color:#008B00'>[👓 镜像地址 ]</span>](https://hub.docker.com/r/jenkins/jenkins ':target=_blank') [<span style='color:#008B00'>[👓 安装说明 ]</span>](https://github.com/jenkinsci/docker/blob/master/README.md ':target=_blank') 
>
>  [ 地址访问`http://192.168.8.144:8080`] <span style='color:Blue'>[ 账号`admin`初始密码通过日志查看`cb890efc284247b588f48956d40e73be`]</span>
>
>
>```tex
>[LF]> *************************************************************
>[LF]> *************************************************************
>[LF]> *************************************************************
>[LF]> 
>[LF]> Jenkins initial setup is required. An admin user has been created and a password generated.
>[LF]> Please use the following password to proceed to installation:
>[LF]> 
>[LF]> cb890efc284247b588f48956d40e73be
>[LF]> 
>[LF]> This may also be found at: /var/jenkins_home/secrets/initialAdminPassword
>[LF]> 
>[LF]> *************************************************************
>[LF]> *************************************************************
>[LF]> *************************************************************
>
>
>```
>
>⚠<span style='color:red'> [ 第一次不要安装任何插件，选择`选择插件来安装`然后直接关闭页面 - 跳过配置即可 ]</span>
>
>---
>
><span style='color:Blue'>[ 如何让`jenkins`操作`Docker`]</span>
>
>- (`Jenkins`) 容器内通过挂载`/usr/bin/docker + /var/run/docker.sock`[ 可以直接调用宿主机的`Docker`服务 + 命令 ]
>
>  <span style='color:red'>[ 无需在容器内单独安装`Docker`或使用`Docker-in-Docker`]</span>
>
>⚠ (`Jenkins`) 容器内执行`docker run -v ...`时<span style='color:red'> [ 挂载的源路径(`如 /some/path`) 指的是`Jenkins`容器内的路径，不是宿主机的路径 ]</span>
>
>⚠ <span style='color:red'>如果需要让目标容器访问宿主机的文件 [ 必须先将宿主机目录挂载到`Jenkins`容器，再由`Jenkins`容器挂载到目标容器 ]</span>
>
>```shell
># [ 查看 ] 宿主机 Docker 安装路径
>[root@localhost jenkins-jdk17] which docker
># /usr/bin/docker
>
>
>```
>
>
>
><br/>

```yaml
services:
   jenkins:
      image: jenkins/jenkins:jdk17
      container_name: jenkins
      user: root         # 指定容器以 root 身份运行
      privileged: true   # 让容器以特权模式运行 ( 容器将拥有更多宿主机权限 )
      ports:
         - "8080:8080"    # WEB
         - "50000:50000"  # 构建代理 ( 用于执行构建任务的工作节点 )
    
      volumes:
         - jenkins_home:/var/jenkins_home
         - /var/run/docker.sock:/var/run/docker.sock
         - /usr/bin/docker:/usr/bin/docker
      
volumes:
   jenkins_home:


```



## ✴ 修改初始密码 :id=H2_1

![image-20251102153923724](wwwroot\DocImage\image-20251102153923724.png ':size=400')



## ✴ 调整默认时区 :id=H2_2

![image-20251102154326981](wwwroot\DocImage\image-20251102154326981.png ':size=400')



## ✴ 设置国内资源 :id=H2_3

!> [ 清华园`https://mirrors.tuna.tsinghua.edu.cn/jenkins/updates/update-center.json`] <span style='color:red'>[ 此地址已失效 ]</span>

![image-20251102154816739](wwwroot\DocImage\image-20251102154816739.png ':size=400')

