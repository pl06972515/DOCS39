<br/>

>[!WARNING|style: flat|label: 简要说明 ]
>
>- <span style='color:Blue'>[ 流水线`Pipeline`] 用于定义和管理持续集成/持续交付 (`CI/CD`) 流水线 ( 它允许开发者通过代码的方式描述构建、测试、部署等流程`Jenkinsfile`)</span>
>
>  [ 插件`Pipeline`] [<span style='color:#008B00'>[👓 插件地址 ]</span>](https://plugins.jenkins.io/workflow-aggregator/ ':target=_blank') [<span style='color:#008B00'>[👓 官方步骤说明 ]</span>](https://www.jenkins.io/doc/pipeline/steps/ ':target=_blank')
>
>
>
><br/>

```shell
pipeline {
    agent any 
    
    # [ 全局: 环境变量 ]
    # - 用途：常用于配置、凭据、路径等
    # - 访问方式：${env.变量名} ( 在步骤 sh 中直接使用 ${变量名} )
    environment {
        EN1 = 'Q1'
        EN2 = 'Q2'
    }
    
    # [ 用户参数 ]
    # - 用途：在手动或定时触发流水线时 [ 允许用户输入或选择参数- 如分支、版本号、姓名等 ]
    # - 设置时机：在流水线启动时由用户填写或选择
    # - 访问方式：${params.参数名} ( 在步骤 sh 中无法直接使用 - 需通过环境变量 )
    parameters {
        string(name:"Name", defaultValue:"SYL", description: "姓名")
        string(name: "Age", defaultValue: "18", description: "年龄（请输入数字）")
    }
    
    stages {
    
        # [ 阶段定义 ]
        stage('第一阶段') {
            # [ 局部: 环境变量 ] 
            environment {
                EN1 = 'Q1'
                EN2 = 'Q2'
            }
            # [ 阶段步骤 ] 定义本阶段要执行的脚本
            steps {
            
                echo 'Hello World'
                echo "环境变量 EN1 = ${env.EN1} EN2 = ${env.EN2}"
                echo "用户参数 姓名: ${params.Name} 年龄 = ${params.Age}"
                
                sh 'echo "环境变量 EN1 = ${EN1} EN2 = ${EN2}"'
                
                script {
                    env.Name = params.Name
                    env.Age = params.Age
                }
                sh 'echo "用户参数 姓名: ${Name} 年龄 = ${Age}"'
                
            }
        }
        
        stage('第二阶段') {
            steps {
               ......
            }
        }
        
        stage('第三阶段：条件阶段') {
        
            when {
    			environment name: 'EN1', value: 'Q1'
			}
            steps {
               ......
            }
        }
        
    }
    
}


```

<!-- tabs:start -->

#### **[ 🟢 管道任务 ] Dockerfile**

!> <span style='color:red'>[ 建议：先通过普通任务测试，在集成`Jenkinsfile`]</span> [<span style='color:#008B00'>[👓 Q1 ]</span>](T1.0.0 ':target=_blank')

```shell
pipeline {
    agent any 
    environment {
    
		ASPNETCORE_ENVIRONMENT = 'Production'
		ASPNETCORE_HTTP_PORTS = '7008'
		ASPNETCORE_HTTPS_PORTS = '8081'
        PUBLISH_HOST = 'http://192.168.8.144'
        
        Habor_HOST = '192.168.8.144:8087'
        Habor_LOGIN = 'admin'
        Habor_PASSWORD = '372577325'
        Habor_PUSH = 'pm2-services'
        
    }
	
    stages {
      
        stage('【 阶段：Gitea 】 代码拉取') {
            steps {
                git credentialsId: '9c2d1561-b171-498b-8042-2fd62719a055', url: 'http://192.168.8.144:3000/pm2-zb-services/testWebApp.git'
            }
        }
		
		stage('【 阶段：Dockerfile 】 构建镜像') {
            steps {
                sh 'docker build --no-cache -f ./testWebApp/Dockerfile -t testwebapp:${BUILD_NUMBER} ./testWebApp'
				sh 'docker image prune -f'
            }
        }
		
		stage('【 阶段：Habor 】 推送镜像') {
            steps {
                sh 'docker login ${Habor_HOST} -u ${Habor_LOGIN} -p ${Habor_PASSWORD}'
				sh 'docker tag testwebapp:${BUILD_NUMBER} ${Habor_HOST}/${Habor_PUSH}/testwebapp:${BUILD_NUMBER}'
                sh 'docker push ${Habor_HOST}/${Habor_PUSH}/testwebapp:${BUILD_NUMBER}'
            }
        }
        
        /*
		stage('[ Docker ] 容器启动') {
            steps {
				sh 'docker rm -f testwebapp || true'
                sh '''
					docker run -d --name testwebapp \
					           --restart=always \
							   -p ${ASPNETCORE_HTTP_PORTS}:${ASPNETCORE_HTTP_PORTS} \
							   -e ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT} \
							   -e ASPNETCORE_HTTP_PORTS=${ASPNETCORE_HTTP_PORTS} \
							   testwebapp:${BUILD_NUMBER}
				'''
            }
        }

		stage('【 阶段：Docker 】 启动日志') {
            steps {		   
				sh 'sleep 1'
				sh 'docker logs --tail 20 testwebapp'
            }
        }

        stage('【 阶段：Health 】健康检查') {
            steps {
                script {
                    def result = sh(script: "curl -sf ${PUBLISH_HOST}:${ASPNETCORE_HTTP_PORTS}/health || exit 1", returnStatus: true)
                    if (result != 0) {
                        error("健康检查失败，容器未正常启动")
                    }
                }
            }
        }
        */
        
    }
    
}


```



#### **[ ☢ 脚本管理 ] Gitea → Jenkinsfile**

!> <span style='color:red'>[ 项目根目录：创建流水线脚本`Jenkinsfile`→ 提交至`Gitea`]</span>

![image-20251103014128159](wwwroot\DocImage\image-20251103014128159.png ':size=400')



!> <span style='color:red'>[ 配置`Jenkins.Pipeline`通过`Gitea`拉取即可 ]</span>

![image-20251103014405258](wwwroot\DocImage\image-20251103014405258.png ':size=400')

<!-- tabs:end -->

